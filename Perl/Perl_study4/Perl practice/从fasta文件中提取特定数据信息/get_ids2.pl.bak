$filename="p53.fasta";
open(RES,">p53_ids.txt");
open(T,"$filename")||die "Cannot open file $filename!\n";
while($line=<T>)
#             >sp|P61981|1433G_HUMAN 14-3-3 protein gamma OS=Homo sapiens GN=YWHAG PE=1 SV=2
{
	if($line=~m/^>sp\|(.*)\|(.*)\_(.*) [0-9]/)
	{
		$id=$1;
		$sp1=$2;
		$sp2=$3;
		print RES "$id--$sp1--$sp2";
		print RES "\n";
		
		
		}
	
	}
close(RES);	close(T);